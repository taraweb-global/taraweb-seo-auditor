venv/
.venv/
.pytest_cache/
__pycache__/
*.pyc
*.pyo
.env*
!.env.example
logs/
*.log
*.db
*.sqlite
*.bundle
.DS_Store
node_modules/
.vscode/
.idea/
# Saved crawls — kept LOCAL ONLY, never pushed to git.
# Stored at ~/.site-crawler-crawls/ (outside this repo). These patterns
# are belt-and-braces in case the storage path ever changes to relative.
.site-crawler-crawls/
site-crawler-crawls/
crawls/

# Windows installer artifacts — generated by install-windows.ps1 into
# the working tree. Without these git status sees the repo as "dirty"
# and the in-app Update button refuses to pull.
Run.ps1
Stop.ps1
Update.ps1
Autostart.ps1
ACCESS_URLS.txt
autostart.log
install.log
install.ps1
